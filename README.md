# TestTask-giper.fm

Тестовое задание для практики в giper.fm, реализованное на современном технологическом стеке React + TypeScript + Vite.

## 📋 Описание проекта

**TestTask-giper.fm** — это веб-приложение, демонстрирующее навыки работы с React и современными инструментами разработки. Проект включает два основных функциональных модуля: систему управления задачами (Todo List) и просмотрщик постов с пагинацией.

### Основные возможности

- ✅ **Управление задачами** — создание, редактирование, удаление и отметка выполнения задач
- 📝 **Просмотр постов** — загрузка и отображение постов с JSONPlaceholder API
- 💾 **Локальное хранение** — сохранение задач в localStorage браузера
- 📄 **Пагинация** — постраничная навигация по постам
- 🔄 **Ленивая загрузка** — подгрузка дополнительных постов по требованию
- 📱 **Адаптивный дизайн** — корректное отображение на всех устройствах

## 🛠 Технологический стек

### Основные технологии
- **React 19** — современная версия библиотеки для создания пользовательских интерфейсов
- **TypeScript** — типизированный JavaScript для повышения надежности кода
- **Vite** — быстрый инструмент сборки и dev-сервер

### Инструменты разработки
- **ESLint** — линтинг кода с поддержкой TypeScript и React
- **TypeScript ESLint** — расширенные правила для TypeScript
- **React Hooks ESLint Plugin** — правила для React Hooks
- **React Refresh ESLint Plugin** — поддержка Hot Module Replacement

### Стилизация
- **CSS Modules** — изолированные стили компонентов
- **CSS Grid & Flexbox** — современная верстка

### Хостинг
- **Netlify** — автоматический деплой из GitHub

## 📁 Структура проекта

```
src/
├── components/           # React компоненты
│   ├── UI/              # Переиспользуемые UI компоненты
│   │   ├── Button.tsx   # Компонент кнопки
│   │   └── Input.tsx    # Компонент поля ввода
│   ├── TaskBoard.tsx    # Компонент доски задач
│   └── PostsViewer.tsx  # Компонент просмотра постов
├── styles/              # CSS Modules стили
│   ├── AppTabs.module.css      # Стили для табов
│   ├── PostsViewer.module.css  # Стили для просмотра постов
│   ├── Shared.module.css       # Общие стили
│   └── TaskBoard.module.css    # Стили для доски задач
├── types/               # TypeScript типы
│   └── models.ts        # Интерфейсы данных
├── assets/              # Статические ресурсы
│   └── react.svg        # SVG иконка React
├── App.tsx              # Главный компонент приложения
├── App.css              # Глобальные стили приложения
├── index.css            # Базовые стили
└── main.tsx             # Точка входа приложения
```

## 🚀 Установка и запуск

### Предварительные требования
- Node.js версии 18 или выше
- npm или yarn

### Установка зависимостей
```bash
npm install
```

### Запуск в режиме разработки
```bash
npm run dev
```
Приложение будет доступно по адресу `http://localhost:5173`

### Сборка для продакшена
```bash
npm run build
```

### Предварительный просмотр сборки
```bash
npm run preview
```

### Проверка кода
```bash
npm run lint
```

## 📊 Функциональность

### 1. Система управления задачами (TaskBoard)

#### Возможности:
- ➕ **Создание задач** — добавление новых задач через поле ввода
- ✅ **Отметка выполнения** — переключение статуса задачи
- 🗑️ **Удаление задач** — удаление ненужных задач
- 💾 **Автосохранение** — автоматическое сохранение в localStorage

#### Технические детали:
- Использует `crypto.randomUUID()` для генерации уникальных ID
- Состояние синхронизируется с localStorage
- Валидация пустых задач

### 2. Просмотрщик постов (PostsViewer)

#### Возможности:
- 📄 **Загрузка постов** — получение данных с JSONPlaceholder API
- 📊 **Табличное отображение** — структурированный просмотр данных
- 🔢 **Пагинация** — навигация по страницам (5 постов на страницу)
- ⬇️ **Ленивая загрузка** — подгрузка дополнительных постов
- ⚠️ **Обработка ошибок** — отображение ошибок загрузки

#### Технические детали:
- Использует `fetch` API для HTTP-запросов
- Реализована пагинация с кнопками и "Загрузить ещё"
- Состояние загрузки и обработка ошибок
- Кэширование загруженных страниц

## 🎨 Компоненты

### UI Компоненты

#### Button
```typescript
interface Props extends React.ButtonHTMLAttributes<HTMLButtonElement> {
  label: string;
}
```
- Переиспользуемый компонент кнопки
- Поддержка всех стандартных HTML-атрибутов
- Единообразное стилевое оформление

#### Input
```typescript
interface Props extends React.InputHTMLAttributes<HTMLInputElement> {}
```
- Переиспользуемый компонент поля ввода
- Поддержка всех стандартных HTML-атрибутов
- Консистентный дизайн

### Основные компоненты

#### TaskBoard
- Управление состоянием задач через `useState`
- Синхронизация с localStorage через `useEffect`
- CRUD операции для задач

#### PostsViewer
- Работа с внешним API
- Управление состоянием загрузки и ошибок
- Реализация пагинации и ленивой загрузки

## 📋 Типы данных

### Task
```typescript
interface Task {
  id: string;      // Уникальный идентификатор
  title: string;   // Название задачи
  done: boolean;   // Статус выполнения
}
```

### Article
```typescript
interface Article {
  userId: number;  // ID пользователя
  id: number;      // ID поста
  title: string;   // Заголовок поста
  body: string;    // Содержание поста
}
```

## 🔄 Архитектурные решения

### Управление состоянием
- **Локальное состояние** — `useState` для компонентного состояния
- **localStorage** — для персистентности данных задач
- **API состояние** — управление загрузкой и ошибками для постов

### Стилизация
- **CSS Modules** — изоляция стилей компонентов
- **Модульная архитектура** — отдельные CSS файлы для каждого компонента
- **Общие стили** — Shared.module.css для переиспользуемых элементов

### Обработка данных
- **Типизация** — полная типизация всех данных через TypeScript
- **Валидация** — проверка данных перед обработкой
- **Обработка ошибок** — graceful handling ошибок API

## 🌐 API Integration

### JSONPlaceholder API
- **Endpoint**: `https://jsonplaceholder.typicode.com/posts`
- **Параметры**: `_limit` и `_page` для пагинации
- **Формат**: JSON с полями userId, id, title, body

### Особенности реализации
- Обработка HTTP ошибок
- Состояние загрузки
- Кэширование загруженных страниц
- Ленивая загрузка дополнительных данных

## 🚀 Деплой

### Автоматический деплой на Netlify
- **URL**: [https://testtask-giperfm.netlify.app/](https://testtask-giperfm.netlify.app/)
- **Интеграция с GitHub** — автоматический деплой при push в main ветку
- **Build команда**: `npm run build`
- **Publish directory**: `dist`

### Локальная сборка
```bash
# Сборка проекта
npm run build

# Предварительный просмотр
npm run preview
```

## 🧪 Тестирование

### Ручное тестирование
- ✅ Создание, редактирование и удаление задач
- ✅ Сохранение задач в localStorage
- ✅ Загрузка постов с API
- ✅ Пагинация и ленивая загрузка
- ✅ Обработка ошибок сети
- ✅ Адаптивность на разных устройствах

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой функции (`git checkout -b feature/amazing-feature`)
3. Зафиксируйте изменения (`git commit -m 'Add some amazing feature'`)
4. Отправьте в ветку (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

## 📝 Лицензия

Этот проект создан в рамках тестового задания для giper.fm.

## 🔗 Ссылки

- [Исходный репозиторий](https://github.com/CeYn97/TestTask-giper.fm)
- [Демо версия](https://testtask-giperfm.netlify.app/)
- [JSONPlaceholder API](https://jsonplaceholder.typicode.com/)

---

*Создано с ❤️ для демонстрации навыков React разработки*
